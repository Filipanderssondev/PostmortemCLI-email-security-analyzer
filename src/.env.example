# .env.example
# Shows available configuration options
# Never committed – copy to .env and fill in locally

# Override container image (e.g. private registry):
# export POSTMORTEM_IMAGE=your-registry/your-image:tag